import { defineMessages } from '@i18n'

export const setupDialogMessages = defineMessages({
	title: {
		id: 'setup.dialog.title',
		defaultMessage: 'Set up your puzzle',
		description:
			'Heading of the dialog holding the Setup choices on a narrow viewport; it also names the dialog',
	},
})
