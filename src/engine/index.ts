export { applyMove, createBoard, isSolved, movableTiles, movesForCell, toPlacements } from './board'
export { shuffle } from './shuffle'
export { GAP } from './types'
export type { Board, CellIndex, Move, TileId, TilePlacement } from './types'
