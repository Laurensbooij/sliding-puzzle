import { defineMessages } from '@i18n'

export const appHeaderMessages = defineMessages({
	settings: {
		id: 'app-header.settings',
		defaultMessage: 'Settings',
		description:
			'Accessible name and tooltip of the header control that opens the settings dialog; the icon is a gear',
	},
})
