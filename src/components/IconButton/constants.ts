export const ICON_BUTTON_TESTIDS = {
	BASE: 'icon-button',
	TOOLTIP_SUFFIX: '-tooltip',
} as const

/** Variants of the Figma component set, in its own order. */
export const ICON_BUTTON_VARIANTS = ['solid', 'outline', 'ghost', 'onWood'] as const

/** Sizes of the Figma component set: sm 32 · md 40 · lg 48. */
export const ICON_BUTTON_SIZES = ['sm', 'md', 'lg'] as const
