# Vendored skill assets — hash-tracked by skills-lock.json, must stay byte-identical
.agents/
pnpm-lock.yaml
dist
coverage
storybook-static
src/styles/tokens.css

# Agent worktrees — whole checkouts of this repo, each formatted on its own branch
.claude/worktrees/
